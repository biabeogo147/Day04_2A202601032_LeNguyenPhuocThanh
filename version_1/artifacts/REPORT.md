# Version 1 Evaluation Report

## Agent

LangGraph ReAct advisor grounded only in `shared_data/DataTPCN.csv`.

## Metrics

Điền kết quả từ `run_eval.py`: completion, routing, safety conflict recall,
grounding/provenance và guardrail violations.

## Failure analysis

Chỉ tạo version tiếp theo khi một run JSON chứng minh failure cụ thể.
