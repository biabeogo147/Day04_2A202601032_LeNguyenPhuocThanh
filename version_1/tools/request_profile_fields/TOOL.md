# request_profile_fields

Pause the LangGraph run when required conversation-context fields are missing.
Input contains canonical context field names and one concise question. Resume
only after the context has been merged into the session through the API.
