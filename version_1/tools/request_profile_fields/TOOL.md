# request_profile_fields

Pause the LangGraph run when required profile fields are missing. Input contains
canonical profile field names and one concise question. Resume only after the
profile has been patched through the API.
